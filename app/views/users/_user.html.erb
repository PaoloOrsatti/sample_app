
<li>
	<%= gravatar_for user, :size => 30 %>
	<%= link_to user.name, user %>
	<% if current_user.admin? %>
    |  <%= button_to "delete", user,  	:method => :delete, 
    :confirm => "Do you really want to delete #{user.name} from the list?" %> 

	<% end %>							
</li>
